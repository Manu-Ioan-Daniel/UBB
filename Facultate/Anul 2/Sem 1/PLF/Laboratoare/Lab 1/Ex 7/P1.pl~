apartine([H|_], H) :- !.
apartine([_|T], E) :- apartine(T, E).

reuniune([], L, L):-!.
reuniune([H|T], L, Rez) :-
    apartine(L, H),
    reuniune(T, L, Rez),!.
reuniune([H|T], L, [H|RezT]) :-
    \+ apartine(L, H),
    reuniune(T, L, RezT).


