CREATE TABLE friendrequests(
	fromId BIGINT NOT NULL REFERENCES Users(userId) ON DELETE CASCADE,
	toId BIGINT NOT NULL REFERENCES Users(userId) ON DELETE CASCADE,
	PRIMARY KEY (fromId,toId),
	status VARCHAR(100) NOT NULL
)
